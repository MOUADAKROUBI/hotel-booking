import React from 'react'
import { useParams } from 'react-router-dom';

export default function SingleCategory() {
    const { id } = useParams();
    return (
        <>
            
        </>
    )
}
